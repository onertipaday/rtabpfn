% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/embeddings.R
\name{tabpfn_embeddings_cv}
\alias{tabpfn_embeddings_cv}
\title{Cross-Validated Embeddings from TabPFN}
\usage{
tabpfn_embeddings_cv(
  x,
  y,
  n_folds = 5L,
  folds = NULL,
  aggregate = "mean",
  mode = "classification",
  ...
)
}
\arguments{
\item{x}{A data.frame, tibble, data.table, or matrix of predictors.}

\item{y}{A factor/character (classification) or numeric (regression) vector.}

\item{n_folds}{Integer. Number of folds when \code{folds} is NULL. Default \code{5L}.}

\item{folds}{\code{NULL} (auto-split), an \code{rsample} rset object, or a list of
\code{list(train = integer_idx, validation = integer_idx)}.}

\item{aggregate}{Character. \code{"mean"} (default), \code{"concat"}, or \code{"none"}.
See \code{\link[=tabpfn_embeddings]{tabpfn_embeddings()}}.}

\item{mode}{Character. \code{"classification"} (default) or \code{"regression"}.}

\item{...}{Additional arguments passed to \code{\link[=tabpfn_classifier]{tabpfn_classifier()}} or
\code{\link[=tabpfn_regressor]{tabpfn_regressor()}}.}
}
\value{
Same format as \code{\link[=tabpfn_embeddings]{tabpfn_embeddings()}}, with rows in original order.
}
\description{
Splits data into K folds, fits a fresh TabPFN on each training fold, and
extracts embeddings for the held-out validation fold. Each sample is
embedded by a model that was NOT trained on it.
}
\examples{
\dontrun{
emb <- tabpfn_embeddings_cv(iris[, 1:4], iris$Species, n_folds = 5)
dim(emb)
}

}
