% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/embeddings.R
\name{tabpfn_embeddings}
\alias{tabpfn_embeddings}
\title{Extract Embeddings from a Fitted TabPFN Model}
\usage{
tabpfn_embeddings(object, new_data, data_source = "test", aggregate = "mean")
}
\arguments{
\item{object}{A fitted \code{tabpfn_classifier} or \code{tabpfn_regressor}.}

\item{new_data}{A data.frame, tibble, data.table, or matrix of observations.}

\item{data_source}{Character. \code{"test"} (default) or \code{"train"} -- which
transformer tokens to return.}

\item{aggregate}{Character. How to aggregate across estimators:
\code{"mean"} (default) averages, \code{"concat"} concatenates, \code{"none"} returns
the raw 3-D array.}
}
\value{
A matrix \verb{(n_samples, embedding_dim)} for \code{"mean"} or \code{"concat"},
or a 3-D array \verb{(n_estimators, n_samples, embedding_dim)} for \code{"none"}.
}
\description{
Returns the transformer's internal representations for each observation.
}
\examples{
\dontrun{
fit <- tabpfn_classifier(iris[, 1:4], iris$Species, n_estimators = 4L)
emb <- tabpfn_embeddings(fit, iris[1:10, 1:4])
dim(emb)
}

}
