% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-embeddings.R
\name{plot_embeddings}
\alias{plot_embeddings}
\title{Plot Embeddings with t-SNE or UMAP}
\usage{
plot_embeddings(
  embeddings,
  labels = NULL,
  method = c("tsne", "umap"),
  return_coords = FALSE,
  ...
)
}
\arguments{
\item{embeddings}{A numeric matrix \verb{(n_samples, embedding_dim)} as
returned by \code{\link[=tabpfn_embeddings]{tabpfn_embeddings()}} with \code{aggregate = "mean"} or
\code{"concat"}.}

\item{labels}{Optional vector (factor, character, or numeric) of length
\code{nrow(embeddings)} used to colour points.}

\item{method}{Character. \code{"tsne"} (default, requires \pkg{Rtsne}) or
\code{"umap"} (requires \pkg{uwot}).}

\item{return_coords}{Logical. If \code{TRUE}, return a list with \verb{$plot}
(ggplot) and \verb{$coords} (tibble). Default \code{FALSE}.}

\item{...}{Additional arguments forwarded to \code{\link[ggplot2:geom_point]{ggplot2::geom_point()}}
(e.g. \code{size}, \code{alpha}, \code{shape}).}
}
\value{
A ggplot2 object, or (when \code{return_coords = TRUE}) a list with
elements \code{plot} and \code{coords}.
}
\description{
Reduces a high-dimensional embedding matrix to 2-D and produces a
ggplot2 scatter plot.
}
\examples{
\dontrun{
fit <- tabpfn_classifier(iris[, 1:4], iris$Species)
emb <- tabpfn_embeddings(fit, iris[, 1:4], data_source = "train")
plot_embeddings(emb, labels = iris$Species, method = "tsne")
plot_embeddings(emb, labels = iris$Species, method = "umap",
                return_coords = TRUE)
}

}
