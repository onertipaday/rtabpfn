% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classifier.R
\name{tabpfn_classifier}
\alias{tabpfn_classifier}
\title{Fit a TabPFN Classifier}
\usage{
tabpfn_classifier(
  x,
  y,
  n_estimators = 8L,
  softmax_temperature = 0.9,
  balance_probabilities = FALSE,
  device = "auto",
  model_version = NULL,
  random_state = 0L,
  ...
)
}
\arguments{
\item{x}{A data.frame, tibble, data.table, or matrix of predictors.}

\item{y}{A factor, character, or integer vector of class labels.}

\item{n_estimators}{Integer. Number of ensemble members. Default \code{8L}.}

\item{softmax_temperature}{Double. Temperature for softmax. Default \code{0.9}.}

\item{balance_probabilities}{Logical. Balance class probabilities.
Default \code{FALSE}.}

\item{device}{Character. Device for computation: \code{"auto"}, \code{"cpu"}, or
\code{"cuda"}. Default \code{"auto"}.}

\item{model_version}{Character or \code{NULL}. TabPFN model version:
\code{"v2"}, \code{"v2.5"}, \code{"v2.6"}, or \code{NULL} (default, uses v2.6).}

\item{random_state}{Integer. Random seed. Default \code{0L}.}

\item{...}{Additional arguments passed to the Python \code{TabPFNClassifier}.}
}
\value{
An object of class \code{"tabpfn_classifier"}.
}
\description{
Trains a TabPFN classifier on tabular data. TabPFN is a transformer-based
model that performs in-context learning on the training data.
}
\examples{
\dontrun{
fit <- tabpfn_classifier(iris[, 1:4], iris$Species)
predict(fit, iris[1:10, 1:4])
predict(fit, iris[1:10, 1:4], type = "prob")
}

}
