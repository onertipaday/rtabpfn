% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/embeddings.R
\name{extract_embeddings}
\alias{extract_embeddings}
\title{Extract Embeddings from a parsnip or workflow Fit}
\usage{
extract_embeddings(object, new_data, ...)
}
\arguments{
\item{object}{A fitted parsnip \code{model_fit} or \code{workflow} object.}

\item{new_data}{A data.frame, tibble, data.table, or matrix.}

\item{...}{Forwarded to \code{\link[=tabpfn_embeddings]{tabpfn_embeddings()}} (e.g. \code{data_source},
\code{aggregate}).}
}
\value{
Same as \code{\link[=tabpfn_embeddings]{tabpfn_embeddings()}}.
}
\description{
Convenience wrapper that unwraps a parsnip \code{model_fit} or a
\code{workflows} fit to reach the underlying TabPFN engine, then calls
\code{\link[=tabpfn_embeddings]{tabpfn_embeddings()}}.
}
\examples{
\dontrun{
library(parsnip)
library(workflows)
library(recipes)
spec <- tabpfn_model(mode = "classification", n_estimators = 4L)
rec  <- recipe(Species ~ ., data = iris)
wf   <- workflow() |> add_recipe(rec) |> add_model(spec)
wf_fit <- fit(wf, data = iris)
emb <- extract_embeddings(wf_fit, iris[1:10, 1:4])
}

}
